'use client';

import React from 'react';

export default function BootSequence() {
    return (
        <div className="fixed inset-0 z-50 pointer-events-none flex items-center justify-center">
            {/* Placeholder for boot sequence animation */}
        </div>
    );
}
